 var integerString = "24"
 var num = Number(integerString);
console.log(integerString);

// we can also use alert instead of console.log
//just like this example
 alert(integerString)

 var floatingNumString = "24.9876";
 var num = Number(floatingNumString);
 console.log(floatingNumString);
// we can also use alert instead of console.log
// just like this example
alert (floatingNumString)


 var numberAsNumber = 1234;
 var numberAsString = numberAsNumber.toString();
 console.log(numberAsNumber.toString());

 // we can also use alert instead of console.log
 //just like this example
 alert(numberAsNumber.toString())